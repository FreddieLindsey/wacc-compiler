#!/usr/bin/ruby
# Ruby front-end for your compiler.
ARGV.each do|a|
    system("java -cp bin:lib/antlr-4.4-complete.jar wacc.Main < #{a}")
    if ($?.exitstatus != 0)
        exit $?.exitstatus
    end
end
